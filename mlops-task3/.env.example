# Database
DB_HOST=localhost
DB_PORT=5433
DB_NAME=olist
DB_USER=olist
DB_PASSWORD=olist123

# MLflow
MLFLOW_TRACKING_URI=http://localhost:5000

# API
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO

# Log files (optional; default: logs/ under the project). The test suite
# points these at a temp directory so it never touches the production logs.
# PREDICTION_LOG_FILE=logs/predictions.jsonl
# SERVICE_LOG_FILE=logs/service.log

# The model version is not configured: it is a hash of the artifacts
# (src/artifacts.py), reported by /health.
